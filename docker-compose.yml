services:
  deadline_checker:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: deadline_checker_container
    image: deadline_checker:latest
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CREDENTIALS_FILE=${CREDENTIALS_FILE}
      - DATABASE_PATH=${DATABASE_PATH}
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./.config:/app/.config
      - ./logs:/app/logs
    networks:
      - deadline_checker_network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "python", "-c", "import sqlite3; sqlite3.connect('../data/bot_data.db')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  deadline_checker_network:
    driver: bridge

volumes:
  bot_data:
  config_data: